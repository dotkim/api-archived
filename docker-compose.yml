version: "3.9"
services:
  api:
    build: .
    ports:
      - 8080:8080
      - 8443:8443
    volumes:
      - files:/app/uploads:rw
      - data:/app/requestlogs:rw
      - /etc/letsencrypt/live/:/app/certs:ro
    depends_on:
      - "mongo"
      - "redis"
    links:
      - "mongo:database"
      - "redis:auth"
  mongo:
    image: "mongo:latest"
    volumes:
      - db:/data/db
  redis:
    image: "redis:latest"
volumes:
  data: {}
  files: {}
  db: {}
